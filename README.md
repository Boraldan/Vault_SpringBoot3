#   HashiCorp Vault + Spring Boot : Demo-стенд

- Авторизация через токен (`VAULT_TOKEN`) для доступа к секретам в `secret/postgres`.
- Включение AppRole-аутентификации и получение `role_id` и `secret_id` для доступа к Vault
